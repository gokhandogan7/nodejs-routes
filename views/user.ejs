<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User</title>
  </head>
  <body>
    <% if(id) { %>
      details of <p><%= users[id].name %></p> 
      <%} else {%>
      User Listyyy:
      <% users.forEach(user => { %>
          <p>- <%=user.name%> </p>
      <% }) %>
      <% }%>
    
  </body>
</html>
